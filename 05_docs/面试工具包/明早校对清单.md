# 明早校对清单（2026-08-17 深夜产出的验收流程，按顺序做，约 1.5 小时）

> 昨晚自动产出了一整套面试材料，**数字是行业常识推导的、代码改动未编译验证**——这份清单是你把它们变成"能上场"的最后一步。

## ① 口径数字校对（30 分钟，最优先）

文件：`00_统一口径与生产数字.md`（所有材料的数字唯一来源）

- [ ] 逐个数字过一遍"是否超出你对前司/行业的认知"——**太满就砍半**（如 15 万→8 万）
- [ ] 两个必被追问的自洽点先自测：
  - "日增 120GB 为什么存量才 25TB" → 答：只算系统 2026.01 上线后的在线增量，历史影像仍在医院旧 PACS
  - "80 万报告为什么只有 35 万词项" → 答：分词去重 + 停用词，医学语料词表就这么大
- [ ] 团队 6 人/两周迭代/Zabbix/事故故事（OSS 429）这套设定合不合你意
- [ ] ⚠️ **改数字要同步三处**：`简历/简历修订稿` 的 bullet、`答案库/` 各文件引用、`15_高危Top15答案卡.md`（Top15 卡 7 的"L1 512 条"来自弹药库，砍缓存数字时记得带上）

## ② 容器内编译验证（10 分钟）

启动 OrbStack + Dev Container 后：

- [ ] `cmake --build build` 通过（昨晚改了 4 处代码，都没编译过）：
  - `04_/include+src/cache/RedisCache.*`（mutex + redisSetTimeout）
  - `04_/include/common/RisConfig.h` + `src/main.cpp`（RIS_IO_THREADS 可配置）
  - `04_/include/protocol/TlvCodec.h`（仅注释）
  - `03_/consumer/main.cpp`（BasicConsume 显式 exclusive=false）
- [ ] 顺手跑 `bash scripts/demo_ris.sh` 确认 RIS 正常起来（4 线程日志应显示）

## ③ 简历验收（20 分钟）

文件：`简历/简历修订稿_2026-08-17.md`

- [ ] Part C 二选一：工作经历**升级版**（推荐，化解"角色倒挂"第一问）还是**保守版**
- [ ] 过一遍 Part B 变更日志 15 条，重点看三处**真相修正**（SeriesWork 桥接/小顶堆归属/RAII 措辞）——这些是核码后和实现对齐的，别改回去
- [ ] 出 PDF 二选一：**省事路线**——改 `简历/master/简历内容.md` 后跑 `python3 scripts/build_resume.py`（一键出 PDF+自检）；**原样式路线**——把 Part A 粘贴回你原来的简历工具导出
- [ ] 无论哪条路线，`check_ats.py` 全绿再投（昨晚已证实你现在的 PDF 文本层没问题，**不需要换字体**；脚本是例行保险）

## ④ 背诵优先级（剩余时间）

1. `答案库/16_自我介绍双版本.md`（30 秒 + 2 分钟，背到脱口而出）
2. `答案库/15_高危Top15答案卡.md`（15 张卡）
3. `答案库/14_雷区压力与行为面.md` 的 §16 逐字口径稿（尤其 AI 工具、角色定位、离职原因）
4. `答案库/01_开场定位与黑话翻译.md`（黑话三秒翻译表）
5. 其余按面试邀约的岗位侧重挑着背

## ⑤ 可选项（不急）

- [ ] `答案库/14` §17.6 薪资题的 X/Y/Z 占位要你自己填
- [ ] Wireshark 真抓一次 9090 端口的包留档（12 号文件的抓包故事目前是推演）
- [ ] 认证服务 Login SQL 是拼接不是参数化（弱注入面）——想修就改成 `mysql_stmt` 预处理；不修就按 05 号答案的"诚实口径"讲
- [ ] 06 号文件"发现的问题"里提到的多词查询摘要锚定词小瑕疵（知道即可）
- [ ] 压测（wrk2）——有精力再做，做了数字组就升级成实测

## 昨晚的意外收获（记住这两条，都是好新闻）

1. **你的简历 PDF 文本层本来就是干净的**——之前评审说的"康熙部首 ATS 杀手"是 pypdf 提取假象（poppler/PDFKit 双验证），不用换字体。
2. **消费者 exclusive=false 修复后**，"按积压横向扩容消费者"和事故故事"拉第二个实例分流"从话术变成了真话。
